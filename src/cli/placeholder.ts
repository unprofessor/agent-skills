// Placeholder — will be replaced by real CLI entries in cli-shims task.
// This exists so that `npm run build` has a file to bundle and the
// esbuild glob `src/cli/*.ts` does not expand to nothing.
export {};
