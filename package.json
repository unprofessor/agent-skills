{
  "name": "planr",
  "version": "0.1.0",
  "private": true,
  "description": "planr — trunk-based backlog for multi-agent work",
  "type": "module",
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "build": "esbuild src/cli/*.ts --bundle --platform=node --format=cjs --outdir=dist/cli --external:yaml"
  },
  "dependencies": {
    "yaml": "^2.7.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "esbuild": "^0.25.3",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
